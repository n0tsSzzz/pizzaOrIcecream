# Классификация изображений: Мороженое vs Пицца

Проект для классификации изображений мороженого и пиццы с использованием глубокого обучения (ResNet-18).

## Описание

Этот проект демонстрирует обучение модели классификации изображений на двух классах:
- 🍦 Мороженое (icecream)
- 🍕 Пицца (pizza)

Модель использует предобученную ResNet-18 с дообучением на кастомном датасете.

## Структура проекта

```
hotDog_recognition/
├── dataset/                    # Датасет с изображениями
│   ├── train/                 # Обучающий набор
│   │   ├── icecream/         # 391 изображение
│   │   └── pizza/            # 327 изображений
│   ├── valid/                 # Валидационный набор
│   │   ├── icecream/         # 114 изображений
│   │   └── pizza/            # 94 изображения
│   └── test/                  # Тестовый набор
│       ├── icecream/         # 59 изображений
│       └── pizza/           # 47 изображений
├── icecream_pizza_augs.ipynb  # Основной ноутбук с обучением
└── README.md                  # Этот файл
```

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd hotDog_recognition
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install torch torchvision
pip install torchshow albumentations pandas opencv-python tqdm
```

Или запустите первую ячейку ноутбука `icecream_pizza_augs.ipynb`, которая установит все необходимые библиотеки.

## Использование

### Обучение модели

Откройте ноутбук `icecream_pizza_augs.ipynb` и выполните ячейки по порядку:

1. **Установка зависимостей** - первая ячейка установит все необходимые библиотеки
2. **Загрузка данных** - настройка путей к датасету
3. **Обучение модели** - обучение ResNet-18 на датасете
4. **Тестирование** - оценка точности модели на тестовом наборе

### Предсказание на новом изображении

Используйте функцию `predict_image()`:

```python
predict_image("путь/к/изображению.jpg", model_path="./icecream_pizza_model.pt")
```

Функция выведет:
- Предсказанный класс (icecream или pizza)
- Уверенность в процентах
- Визуализацию изображения с результатом

### Сравнение моделей на разных размерах данных

Ноутбук включает функцию сравнения моделей, обученных на:
- **Маленьком** наборе (10% данных, ~72 изображения)
- **Среднем** наборе (30% данных, ~215 изображений)
- **Большом** наборе (60% данных, ~430 изображений)
- **Полном** наборе (100% данных, ~718 изображений)

## Особенности

- ✅ Использование предобученной ResNet-18
- ✅ Аугментация данных для улучшения обобщения
- ✅ Early stopping для предотвращения переобучения
- ✅ Сравнение моделей на разных размерах данных
- ✅ Визуализация результатов обучения
- ✅ Функция предсказания на новых изображениях